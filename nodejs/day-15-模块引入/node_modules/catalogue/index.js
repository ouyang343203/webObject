console.log('找到了无目录下的涛')

var Crow ={
    name:'我是头母牛',
    sex:'男'
 }

 function prininfo(){
    console.log('找到了母牛')
 }

  //暴露模块内容使用： module.exports={暴露内容} 有且仅有一个module.exports 默认暴露的内容是一个空对象
  module.exports={
    //暴露的内容添加在此处
    myCrow:Crow,
    myInfo:prininfo,
}
